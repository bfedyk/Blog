<!-- Add the header using EJS partials-->
<%- include("partials/header") -%>


<h1>Home</h1>

<!-- Take home start content from app.js-->
<!-- Use EJS scripting -->
<p><%= startContent %></p>

<!-- Log the array 'posts' here-->
<!-- For loop would have script let tags on each line
for (var i = 0; i< posts.length; i++) {
  console.log(posts[i].title);
 } -->

 <% posts.forEach(function(post){ %>
   <h1><%= post.title %></h1>
   <!-- Can use logic to only truncate posts that are > 100 chars -->
   <!-- if (post.content.length > summaryLength){ -->
   <!-- Here we use the variable post.title to create
        the anchor tag which takes us to /posts/:postName
        route in app.js-->
      <p>
        <%= post.content.substring(0, 100) + " ... " %>
        <a href="/posts/<%= post.title %>" >Read more</a>
      </p>

   <!-- } else { -->
     <!-- <p>post.content</p> -->
    <!-- };  -->

 <% }); %>


<!-- Add the footer page using EJS partials-->
<%- include("partials/footer") -%>
